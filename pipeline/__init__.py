"""
pipeline/ - orchestration layer that wires the existing `src/` modules
into one end-to-end raw-PDF -> LLM-extraction run.

This package was added on top of the existing codebase; it does not
modify anything under `src/`. See `pipeline/README.md` and the top-level
`main.py` for usage.
"""
